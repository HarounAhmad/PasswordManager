spring:
  datasource:
    url: jdbc:postgresql://minikube:31000/db
    username: user
    password: password


  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        generate_statistcs: true
    hibernate:
      ddl-auto: update